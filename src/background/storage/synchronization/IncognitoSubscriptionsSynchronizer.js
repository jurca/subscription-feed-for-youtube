
/**
 * Utility for synchronizing the user's incognito subscriptions in the main
 * data storage (Indexed DB) against the modifications reported by the Chrome's
 * (cross-device) synchronized storage.
 */
export default class IncognitoSubscriptionsSynchronizer {}
